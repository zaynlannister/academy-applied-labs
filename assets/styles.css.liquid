:root {
    --heroBg: {{ settings.background_color }};
    --heroTextColor: {{ settings.text_color }};
}

.hero {
    background-color: var(--heroBg);
}

.hero-title, .hero-subtitle {
    color: var(--heroTextColor);
}
